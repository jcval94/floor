risk:
  trading_mode_default: PAPER
  allow_live_only_when_env_true: true
  max_position_notional_usd: 50000
  max_gross_exposure_usd: 500000
  max_single_name_weight: 0.08
  max_sector_weight: 0.25
  daily_loss_limit_bps: 150
  intraday_drawdown_limit_bps: 250
  position_loss_add_block_bps: 150
  position_profit_add_block_bps: 300
  min_drawdown_risk_scale: 0.50
  kill_switch:
    enabled: true
    triggers:
      - excessive_drawdown
      - stale_data
      - order_reject_spike
