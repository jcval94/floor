# Store training/model artifacts directly in Git (non-LFS).
# This keeps champion payloads available without external LFS resolution.
data/training/*.json -filter -diff -merge text
data/training/**/*.json -filter -diff -merge text
