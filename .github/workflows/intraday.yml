name: deprecated_intraday

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Show deprecation notice
        run: |
          echo "Deprecated: use intraday_engine workflow"
